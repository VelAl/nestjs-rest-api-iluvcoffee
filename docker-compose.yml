services:
  db:
    image: postgres
    restart: always
    ports:
      - 5432:5432
    environment:
      POSTGRES_PASSWORD: '123456'
      POSTGRES_DB: 'ilovecoffee-postgres'

# docker compose up -d
